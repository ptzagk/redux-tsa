describe("skurt", () => {

    test("welcome skurt", async () => {
        const ps: Array<Promise<any>> = [
            Promise.resolve(true),
            Promise.resolve(400),
            Promise.resolve("huh"),
        ]
    });
})
